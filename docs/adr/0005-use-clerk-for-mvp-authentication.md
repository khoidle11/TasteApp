# Use Clerk for MVP authentication

TasteApp will use Clerk for MVP authentication with email code plus Google/Apple login, while storing the local TasteApp User profile, reviews, badges, moderation history, and contributor activity in the app database. SMS authentication is intentionally not the primary MVP path because it adds production cost before the product has traction.
