# Use AWS CDK for the first AWS deployment

TasteApp will use Docker Compose for MVP local development and AWS CDK with TypeScript for the first real AWS deployment. Terraform or OpenTofu remain acceptable later if cloud-agnostic infrastructure practice becomes a priority, but CDK fits the current AWS-first and TypeScript-first direction with less early context switching.
