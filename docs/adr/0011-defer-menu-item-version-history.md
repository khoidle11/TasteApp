# Defer Menu Item version history

TasteApp will track MVP Menu Items with active/inactive state, optional last-known price or price range, and a last-seen timestamp, but will not build full Menu Item version history initially. Version history can be added later if menu churn makes old reviews misleading enough to justify the extra model complexity.
