{
  "name": "@tasteapp/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "pnpm --filter @tasteapp/contracts build && pnpm run build && node dist/server.js",
    "lint": "eslint . --max-warnings=0",
    "start": "node dist/server.js",
    "test": "pnpm --filter @tasteapp/contracts build && vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clerk/backend": "^3.11.4",
    "@prisma/client": "^6.19.3",
    "@tasteapp/contracts": "workspace:*"
  },
  "devDependencies": {
    "prisma": "^6.19.3"
  }
}
